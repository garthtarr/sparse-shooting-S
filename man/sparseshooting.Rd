% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparseshooting.R
\name{sparseshooting}
\alias{sparseshooting}
\title{Sparse Shooting S-Estimator}
\usage{
sparseshooting(
  x,
  y,
  k = 3.42,
  maxIteration = 100,
  tol = 10^-2,
  betaEst = NULL,
  intercept = NULL,
  scaleVar = NULL,
  xhat = NULL,
  xtilde = NULL,
  maxituniv = 1,
  maxitscale = 100,
  wvalue = 3,
  shoot_order = "default",
  nlambda = 100,
  post = TRUE,
  standardize = TRUE,
  lambda_grid = NULL,
  predset = NULL
)
}
\arguments{
\item{x}{An \verb{n x p} numeric matrix of predictors.}

\item{y}{A numeric vector of length \code{n} (the response).}

\item{k}{Tuning constant for Tukey's biweight rho function. Default is
\code{3.420}, which yields 85\% efficiency under normality.}

\item{maxIteration}{Maximum number of outer shooting loop iterations.
Default is \code{100}.}

\item{tol}{Convergence tolerance for the outer shooting loop. Default is
\code{1e-2}.}

\item{betaEst}{Optional \verb{p x 1} matrix of initial regression coefficient
estimates. If \code{NULL} (default), initialised internally via an MM-estimator.}

\item{intercept}{Optional \verb{p x 1} matrix of initial intercept estimates.
If \code{NULL} (default), computed alongside \code{betaEst}.}

\item{scaleVar}{Optional numeric vector of length \code{p} containing initial
scale estimates. If \code{NULL} (default), computed internally.}

\item{xhat}{Optional \verb{n x p} matrix of expected (cleaned) predictor values.
If \code{NULL} (default), computed via a robust univariate regression step.}

\item{xtilde}{Optional \verb{n x p} matrix of pre-processed predictor values.
If \code{NULL} (default), derived from \code{xhat}.}

\item{maxituniv}{Maximum number of IRLS iterations per univariate
sub-regression. Default is \code{1} (one-step re-weighting).}

\item{maxitscale}{Maximum number of fixed-point iterations for the M-scale
estimate. Default is \code{100}.}

\item{wvalue}{Cut-off value used for cellwise outlier flagging. Default
is \code{3}.}

\item{shoot_order}{Order in which predictors are cycled. \code{"default"} cycles
in column order \verb{1, ..., p}; \code{"robcor"} orders by decreasing robust
(Kendall) correlation with \code{y}.}

\item{nlambda}{Number of values in the automatically constructed lambda
grid. Default is \code{100}. Ignored when \code{lambda_grid} is supplied.}

\item{post}{Logical. If \code{TRUE} (default), the post-lasso approach is used
(coefficients are not shrunk after support identification).}

\item{standardize}{Logical. If \code{TRUE} (default), predictors are robustly
standardised before fitting and coefficients are back-transformed on
output.}

\item{lambda_grid}{Optional numeric vector of sparsity parameter values to
evaluate. If \code{NULL} (default), a grid is constructed automatically.}

\item{predset}{Optional integer vector specifying the initial predictor
subset used for the MM starting-value fit.}
}
\value{
A list with the following components:
\describe{
\item{\code{coef}}{Numeric vector of length \code{p + 1}: intercept followed by
\code{p} slope coefficients, selected by BIC using the M-scale.}
\item{\code{coef_ln}}{Numeric vector of length \code{p + 1}: alternative
coefficient estimates selected by BIC using the log M-scale.}
\item{\code{weights}}{\verb{n x p} matrix of robustness weights at the optimal
lambda.}
\item{\code{weights_ln}}{\verb{n x p} robustness weights at the log-BIC optimal
lambda.}
\item{\code{lambda_opt}}{Optimal sparsity parameter selected by BIC.}
\item{\code{lambdas}}{Full lambda grid that was evaluated.}
\item{\code{iter}}{Number of outer shooting loop iterations at the optimal
lambda.}
\item{\code{betahats}}{List of slope coefficient vectors, one per lambda.}
\item{\code{alphahats}}{List of intercept values, one per lambda.}
\item{\code{fits}}{Full output of the internal model selection step,
containing results for all lambda values.}
\item{\code{start_flag}}{Logical matrix indicating which predictor cells
were flagged as outliers during initialisation.}
\item{\code{startfit}}{Output of the MM initialisation step.}
\item{\code{xhat}}{Cleaned predictor matrix used internally.}
\item{\code{xtilde}}{Pre-processed predictor matrix used internally.}
}
}
\description{
Computes the sparse shooting S-estimator for robust regression in
high-dimensional settings with cellwise contamination. A BIC criterion
based on the residual M-scale is used to select the sparsity parameter
from a data-driven grid.
}
\examples{
set.seed(1)
n <- 50; p <- 20
X <- matrix(rnorm(n * p), n, p)
y <- X[, 1] + X[, 2] + rnorm(n)
fit <- sparseshooting(x = X, y = y, nlambda = 20)
fit$coef

}
\references{
Bottmer, L., Croux, C., and Wilms, I. (2022). Sparse regression
for large data sets with outliers. \emph{Econometrics and Statistics}.
}
\seealso{
\code{\link[=shooting]{shooting()}} for the non-sparse variant.
}
